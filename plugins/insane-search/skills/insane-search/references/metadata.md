# Metadata Extraction

Even when the page body is weak, inspect:

- Open Graph tags
- JSON-LD
- article metadata
- product metadata

Use metadata when:

- you need title, summary, author, date, price, or product list items
- the page is partially blocked but still exposes structured data

Do not confuse partial metadata with a full successful fetch. Report when the result is metadata-only.
